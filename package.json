{
  "name": "declarative-js",
  "version": "3.0.1-0",
  "main": "index.js",
  "types": "target/dist/definitions/index.d.ts",
  "author": "Pavel Surinin <pavel.surinin@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pavel-surinin/declarative-js.git"
  },
  "devDependencies": {
    "@types/jest": "22.2.3",
    "jest": "22.4.3",
    "ts-jest": "22.4.5",
    "tslint": "5.10.0",
    "typescript": "2.8.3"
  },
  "scripts": {
    "compile": "npx tsc -d",
    "lint": "npx tslint -c tslint.json -p tsconfig.json -t stylish",
    "test": "npx jest -c jest.config.js",
    "build": "npm run lint && npm run compile && npm test -- --coverage",
    "pre-publish": "npm run build && cp package.json target/dist/js/src/package.json"
  }
}
