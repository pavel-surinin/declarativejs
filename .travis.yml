language: node_js
node_js:
  - "20"

before_install:
  - corepack enable
  - corepack prepare pnpm@10.15.0 --activate

install:
  - pnpm install --frozen-lockfile

script:
  - pnpm run test:coverage

after_script:
  - pnpm run coveralls
